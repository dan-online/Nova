set hi as 2;
log hi + 2 + " will be 3";
log 12 / 6 + " will be 2";
log NaN + 1 + " will be NaN";
if 1 == hi then log "hi" else log "uh oh";
log args;
log "options: " + args.filter(arg => arg.startsWith("--"));